# inherits configuration from ../App/config.yml

doctrine:
    dbal:
        driver: pdo_mysql
        host: mariadb
        port: 3306
        dbname: acme
        user: root
        password: root
        server_version: '11'
