# inherits configuration from ../App/config.yml

doctrine:
    dbal:
        driver: pdo_pgsql
        host: postgres
        port: 5432
        dbname: postgres
        user: postgres
        password: postgres
