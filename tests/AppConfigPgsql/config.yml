# inherits configuration from ../App/config.yml

doctrine:
    dbal:
        driver: pdo_pgsql
        host: 127.0.0.1
        port: 5432
        dbname: postgres
        user: postgres
        password: postgres
